# Server Configuration
server.address=0.0.0.0
server.port=${PORT:10000}

# Database Configuration
spring.datasource.url=jdbc:mysql://chatapp-vinaybrothervns-2010.i.aivencloud.com:13792/chatapp?createDatabaseIfNotExist=true&useSSL=true
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_z9J8sA_BJ84wasOTcrO
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
jwt.secret=mySecureJwtSecretKeyThatIsAtLeast32CharactersLong123456
jwt.expiration=86400000

# WebSocket Configuration
websocket.allowed-origins=*
websocket.endpoint=/ws
websocket.topic-prefix=/topic
websocket.application-prefix=/app

# File Upload Configuration
file.upload.directory=uploads
file.base.url=http://localhost:8081/files
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Application Lifecycle - PREVENT AUTO SHUTDOWN
spring.main.keep-alive=true
spring.main.web-application-type=servlet
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=30s

# Connection Pool Settings - PREVENT DB TIMEOUTS
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.connection-timeout=20000

# Logging to help debug shutdown issues
logging.level.org.springframework.boot=INFO
logging.level.org.springframework.context=INFO 
